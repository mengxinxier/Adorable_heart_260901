ELF_FourSeasons = {
    ELF_invite_FROM_add_to_FourSeasons = {
        icon = GFX_ELF_FourSeasons
        allowed = {
            TAG = EWW
        }
        target_trigger = {
            FROM = {
                not_in_any_economy_faction = yes
                is_in_faction_with = EWW
                not = { has_country_flag = ELF_invite_FROM_add_to_FourSeasons }
                not = { has_idea = ELF_FourSeasons_idea }
            }
        }
        available = {
            FROM = {
                not_in_any_economy_faction = yes
                is_in_faction_with = EWW
                not = { has_idea = ELF_FourSeasons_idea }
            }
            not = { has_war_with = FROM }
        }
        cost = 10
        days_remove = 15
        remove_effect = {
            FROM = {
                set_country_flag = ELF_invite_FROM_add_to_FourSeasons
                country_event = {
                    id = EWW_union.0038
                }
            }
        }
        ai_will_do = {
            base = 1000
        }
    }
}