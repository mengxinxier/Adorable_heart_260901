	on_actions = {
#不要修改头尾文件
#除非你知道你在干什么
	on_capitulation = {
        effect = {

		log = "[GetDateText]: FROM: [From.GetName] ROOT: [Root.GetName] on_capitulation"

			set_global_flag = show_peace_popup_alert
			FROM = {
				save_global_event_target_as = winning_country
			}
			ROOT = {
				save_global_event_target_as = losing_country
			}

			log = "[GetDateText]: winner/loser: FROM: [winning_country.GetName] ROOT: [losing_country.GetName] on_capitulation"

			if = {
				limit = {
					event_target:losing_country = {
						any_allied_country = {
							has_war_together_with = event_target:losing_country
						}
					}
				}
				set_global_flag = losing_country_multiple
			}

			if = {
				limit = {
					event_target:winning_country = {
						any_allied_country = {
							has_war_together_with = event_target:winning_country
						}
					}
				}
				set_global_flag = winning_country_multiple
			}
        if = {
			limit = {
				FROM = {
					AND = {
						has_country_flag = MGU_yuki_enter
						OR = {
							tag = MGU
							}
					}
						}
				ROOT = {
				       tag = PAO
					}
			    }
				FROM = {
					white_peace = {
						tag= ROOT
						message = MGU_yuki_win_PAO
					}
					}
				FROM = {
					set_country_flag = MGU_yuki_annex_PAO
					annex_country = {
						target = ROOT
					}
				}
		    }
			if = {
			limit = {
				FROM = {
					AND = {
						has_country_flag = MGU_yuki_enter
						OR = {
							tag = MGU
							}
					}	
						}
				ROOT = {
				       tag = LHS
					}
			    }
				FROM = {
					white_peace = {
						tag= ROOT
						message = MGU_yuki_win_LHS
					}
					}
				FROM = {
					set_country_flag = MGU_yuki_annex_LHS
					annex_country = {
						target = ROOT
					}
				}
		    }
			if = {
			limit = {
				FROM = {
					AND = {
						has_country_flag = MGU_yuki_enter
						OR = {
							tag = MGU
							}
					}
						}
				ROOT = {
				       tag = PAM
					}
			    }
				FROM = {
					white_peace = {
						tag= ROOT
						message = MGU_yuki_win_PAM
					}
					}
				FROM = {
					set_country_flag = MGU_yuki_annex_PAM
					annex_country = {
						target = ROOT
					}
				}
		    }
			if = {
			limit = {
				FROM = {
					AND = {
						has_country_flag = MGU_yuki_enter
						OR = {
							tag = MGU
							}
					}
						}
				ROOT = {
				       tag = PAQ
					}
			    }
				FROM = {
					white_peace = {
						tag= ROOT
						message = MGU_yuki_win_PAQ
					}
					}
				FROM = {
					set_country_flag = MGU_yuki_annex_PAQ
					annex_country = {
						target = ROOT
					}
				}
		    }
		}
	}

	  

	

	on_declare_war = {
		effect = {
			log="[GetDateText]: [Root.GetName] declared war on [From.GetName]"
		}
	}
}