PAM_nightingale_doctrine = {
    folder = land
    
    name = PAM_DOCTRINE_NIGHTINGALE
    description = PAM_DOCTRINE_NIGHTINGALE_DESC
    icon = GFX_youji_on_me14_medium
    available = {
       always = yes
    }
    visible = {
        has_country_flag = PAM_yeying
    }
    xp_cost = 100
    xp_type = army

    ai_will_do = {
        base = 2
    }

    tracks = {
        infantry
        combat_support
        armor
        operations
    }

    enable_tactic = tactic_sf_fortify
    night_movement = 0.05
    land_night_attack = 0.1
    initiative_factor = 0.05
    milestones = {
        {
           additional_brigade_column_size = 1
            recon_factor = 0.20
            dig_in_speed_factor = 0.15

            effect = {
                if = {
                    limit = {
                        NOT = {
                            has_variable = infantry_milestone_var
                        }
                    }
                    set_variable = { infantry_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { infantry_milestone_var = 1 }
                }
            }
        }
        {
            coordination_bonus = 0.05
            supply_consumption_factor = -0.05
            enable_tactic = tactic_counterattack

            effect = {
                if = {
                    limit = {
                        NOT = {
                            has_variable = support_milestone_var
                        }
                    }
                    set_variable = { support_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { support_milestone_var = 1 }
                }
            }
        }
        {
            acclimatization_cold_climate_gain_factor = 0.15
            breakthrough_factor = 0.05
            enable_tactic = tactic_encirclement

            effect = {
                if = {
                    limit = {
                        NOT = {
                            has_variable = armor_milestone_var
                        }
                    }
                    set_variable = { armor_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { armor_milestone_var = 1 }
                }
            }
        }
        {
            planning_decay_rate_factor = -0.05
            max_command_power = 15
            enable_tactic = tactic_backhand_blow

            effect = {
                if = {
                    limit = {
                        NOT = {
                            has_variable = operations_milestone_var
                        }
                    }
                    set_variable = { operations_milestone_var = 1 }
                }
                else = {
                    add_to_variable = { operations_milestone_var = 1 }
                }
            }
        }
    }
}