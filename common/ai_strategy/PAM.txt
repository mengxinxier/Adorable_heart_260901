PAM_start1 = {
    enable = {
        tag = PAM
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = force_defend_ally_borders
        id = "WRQ"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PAO"
        value = -1000
    }
    ai_strategy = {
        type = become_spymaster
        value = 30  # 花费政治点数竞选间谍大师的权重值
    }
    ai_strategy = {
        type = garrison
        value = -200
    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_coastal = yes
            FROM = { has_war_with = FROM.FROM }
        }
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
    }
    ai_strategy = {
        type = building_target
        id = supply_node
        value = 200
    }
    ai_strategy = {
        type = research_weight_factor	#重坦
        id = basic_heavy_tank_chassis
        value = -999
    }
    ai_strategy = {
        type = research_tech	#工业
        id = concentrated_industry
        value = 200
    }
    ai_strategy = {
        type = research_tech	#科技倾向
        id = tech_support
        value = 200
    }
    ai_strategy = {
        type = research_tech	#电子学
        id = electronic_mechanical_engineering
        value = 200
    }
}
NANMEI_management_stop = {
    enable = {
        has_war = yes
        has_manpower < 500
        surrender_progress > 0.3
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        country_trigger = {
            enemies_strength_ratio > 0.4
        }
        ordertype = front
        execution_type = careful
        execute_order = no
        manual_attack = no
    }
}


NANMEI_management_rush_desperation = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
        has_manpower < 500
        surrender_progress > 0.4
        enemies_strength_ratio < 0.8
        NOT = { surrender_progress > 0.6 } # ensures extreme cases go to Stop instead
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}


NANMEI_management_rush_failing = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
        surrender_progress > 0.2
        surrender_progress < 0.4
        has_manpower > 499
        enemies_strength_ratio < 0.9
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}


NANMEI_management_careful_failing = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
        surrender_progress > 0.2
        surrender_progress < 0.4
        has_manpower > 499
        NOT = { enemies_strength_ratio < 0.9 }
        enemies_strength_ratio < 1.3
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        ordertype = front
        execution_type = careful
        execute_order = yes
        manual_attack = yes
    }
}


NANMEI_management_rush = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
        any_enemy_country = { is_major = yes }
        surrender_progress < 0.2
        has_manpower > 499
        enemies_strength_ratio < 1.6
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}


NANMEI_management_careful = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
        any_enemy_country = { is_major = yes }
        surrender_progress < 0.2
        has_manpower > 499
        NOT = { enemies_strength_ratio < 1.6 }
        enemies_strength_ratio < 3.0
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        ordertype = front
        execution_type = careful
        execute_order = yes
        manual_attack = yes
    }
}


NANMEI_management_rush_vs_minors = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
        is_major = yes
        surrender_progress < 0.2
        has_manpower > 499
        enemies_strength_ratio < 3.0
        NOT = { any_enemy_country = { is_major = yes } } # ensures only minors are enemies
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
}


NANMEI_management_careful_vs_minors = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
        is_major = yes
        surrender_progress < 0.2
        has_manpower > 499
        enemies_strength_ratio > 2.5
        enemies_strength_ratio < 4.0
        NOT = { any_enemy_country = { is_major = yes } } # ensures only minors are enemies
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        ordertype = front
        execution_type = careful
        execute_order = yes
        manual_attack = yes
    }
}
PAMMore_Infantry_focus = {#多产枪
    allowed = {
        original_tag = PAM
    }
    enable = {
        arms_factory > 40
        arms_factory < 69
        has_equipment = { infantry_equipment < 1000 }
    }

    abort_when_not_enabled = yes


    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 50
    }

}
PAM_stop_disbanding_your_army = {#ai部署陆军倾向
    allowed = {
        original_tag = PAM
    }
    enable = {
        num_divisions < 800
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 1000
    }
}
PAM_stop_disbanding_your_army2 = {#ai部署陆军倾向2
    allowed = {
        original_tag = PAM
    }
    enable = {
        num_divisions > 800
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = -2000
    }
}
PAMMore_Infantry_focus_2 = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        arms_factory > 70
        arms_factory < 99
        has_equipment = { infantry_equipment < 1000 }
    }
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 75
    }
    abort_when_not_enabled = yes
}



PAMMore_Infantry_focus_3 = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        arms_factory > 100
        has_equipment = { infantry_equipment < 1000 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 125
    }
}




PAMMore_Support_focus = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        arms_factory > 40
        arms_factory < 69
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 25
    }
}

PAMMore_Support_focus_2 = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        arms_factory > 70
        arms_factory < 99
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 50
    }
}
PAMMore_Support_focus_3 = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        arms_factory > 100
        has_equipment = { support_equipment < 100 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_variant_production_factor
        id = support_equipment
        value = 100
    }
}
PAMLessAirForMass = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_doctrine = mass_assault
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type  = air_factory_balance
        value = -25
    }
}


PAMIfyouhavethemusethem2 = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        or = {
            has_equipment = { medium_tank_chassis > 1 }
            has_equipment = {medium_tank_destroyer_chassis > 1}
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = build_army
        id = PAM_armor
        value = 30
    }

}









PAMPushForMoreTONK = {
    allowed = {
        original_tag = PAM
    }
    enable = {

        has_manpower < 1500000
        arms_factory > 45
        has_equipment = {infantry_equipment > 25000}
        
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = role_ratio
        id = PAM_armor
        value = 30
    }
    ai_strategy = {
        type = equipment_production_factor
        id = armor
        value = 20
    }

}

PAMMajorsBuildTanksWithManpower = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        is_major = yes
        has_equipment = {infantry_equipment > 1}
        
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = role_ratio
        id = PAM_armor
        value = 50
    }
    ai_strategy = {
        type = equipment_production_factor
        id = armor
        value = 50
    }
}

#1939年研究
PAM_ai_research = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        date > 1939.1.1
    }
    abort = {
        always = no
    }
    ai_strategy = { #36年步兵装备
        type = research_tech
        id = infantry_weapons1
        value = 2000
    }
    ai_strategy = { #大威力步枪
        type = research_tech
        id = infantry_weapons2
        value = 2000
    }
    ai_strategy = { #40年步兵装备
        type = research_tech
        id = improved_infantry_weapons
        value = 2000
    }
    ai_strategy = { #40年步兵装备2
        type = research_tech
        id = improved_infantry_weapons_2
        value = 2000
    }
    ai_strategy = { #42年步兵装备
        type = research_tech
        id = advanced_infantry_weapons
        value = 2000
    }
    ai_strategy = { #42年步兵装备
        type = research_tech
        id = advanced_infantry_weapons2
        value = 2000
    }
    ai_strategy = { #中坦
        type = research_tech
        id = basic_medium_tank_chassis
        value = 800
    }
    ai_strategy = { #邪恶的坦克
        type = research_tech
        id = basic_heavy_tank_chassis
        value = 800
    }
    ai_strategy = { #火炮
        type = research_tech
        id = gw_artillery
        value = 200
    }
    ai_strategy = { #火炮2
        type = research_tech
        id = interwar_artillery
        value = 200
    }

    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = artillery1
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = artillery2
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = artillery3
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = artillery4
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = antitank1
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = antitank2
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = antitank3
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = antitank4
        value = 200
    }
    ai_strategy = { #工兵连 II
        type = research_tech
        id = tech_engineers2
        value = 200
    }
    ai_strategy = { #维修连 II
        type = research_tech
        id = tech_maintenance_company2
        value = 400
    }
    ai_strategy = { #后勤连 II
        type = research_tech
        id = tech_logistics_company2
        value = 300
    }
}


PAM_motorized_production = { #缺卡车
    allowed = {
        original_tag = PAM

    }
    enable = {
        has_equipment = {
            motorized_equipment < 1000
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = motorized
        value = 10
    }
    ai_strategy = {
        type = equipment_variant_production_factor
        id = motorized_equipment
        value = 50
    }
}
PAM_artillery_production = { #缺炮
    allowed = {
        original_tag = PAM

    }
    enable = {
        has_equipment = {
            artillery_equipment < 200
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = artillery
        value = 5
    }
}
PAM_anti_tank_production = { #缺at
    allowed = {
        original_tag = PAM

    }
    enable = {
        has_equipment = {
            anti_tank_equipment < 100
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = anti_tank
        value = 5
    }
}
PAM_anti_air_production = { #缺对空
    allowed = {
        original_tag = PAM

    }
    enable = {
        has_equipment = {
            anti_air_equipment < 200
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = anti_air
        value = 5
    }
}
PAM_train_production = { #缺火车
    allowed = {
        original_tag = PAM

    }
    enable = {
        has_equipment = {
            train_equipment  < 50
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = train
        value = 8
    }
}
PAM_train_production = { #缺运输船
    allowed = {
        original_tag = PAM

    }
    enable = {
        has_equipment = {
            convoy  < 500
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = convoy
        value = 10
    }
}

PAM_wants_arms_factory = { #搓军工
    allowed = {
        original_tag = PAM
    }
    enable = {
        num_of_military_factories < 300
    }
    abort = {
        num_of_military_factories > 300
    }
    ai_strategy = {
        type = building_target
        id = arms_factory
        value = 100
    }
}







PAM_wants_civ = {#搓民工
    allowed = {
        original_tag = PAM
    }
    enable = {
        date < 1938.9.1
        has_war = no
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = building_target
        id = industrial_complex
        value = 10000
    }
}



PAM_surplus_management = {#存装备

    allowed = {
        original_tag = PAM
    }
    enable = {
        always = yes
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_surplus_management
        id = infantry_equipment
        value = 10
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = support_equipment
        value = 5
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = artillery_equipment
        value = 5
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = anti_air_equipment
        value = 3
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = anti_tank_equipment
        value = 2
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = convoy
        value = 5
    }
}
PAM_market_convoy_purchases = {#买运输船
    allowed = {
        original_tag = PAM
    }
    enable = {
        naval_base > 0
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_market_buying_threshold
        id = convoy
        value = 30
    }
}
PAM_market_stockpile_management_if_at_war = {#战时市场
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_war = yes

    }
    abort_when_not_enabled = yes


    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = infantry
        value = 20000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = support
        value = 6000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = artillery
        value = 1000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = fighter
        value = 200
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = motorized
        value = 1000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = train
        value = 50
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = convoy
        value = 50
    }
}
PAM_stockpile_management = {#装备库存保持
    allowed = {
        original_tag = PAM
    }
    enable = {
        num_divisions > 150
    }

    abort = {
        num_divisions < 130
    }

    ai_strategy = {
        type = equipment_stockpile_surplus_ratio
        value = 30
    }
}

PAM_if_we_can_build_v1_rockets_we_want_rockets = {#生产v1导弹
    allowed = {
        original_tag = PAM
    }
    enable = {
        is_special_project_completed = sp:sp_rockets_flying_bomb
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = guided_missile_equipment
        value = 1
    }
}

PAM_if_we_can_build_v2_rockets_we_want_v2_rockets = {#生产v2导弹
    allowed = {
        original_tag = PAM
    }
    enable = {
        is_special_project_completed = sp:sp_rockets_ballistic_missile
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = ballistic_missile_equipment
        value = 1
    }
}
PAM_if_we_can_build_sams_we_want_sams = { #生产防空导弹
    allowed = {
        original_tag = PAM
    }
    enable = {
        is_special_project_completed = sp:sp_rockets_ground_to_air_missile
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = sam_missile_equipment
        value = 1
    }
}
PAM_if_we_can_build_nuclear_missiles_we_want_nuclear_missiles = {#生产核弹
    allowed = {
        original_tag = PAM
    }
    enable = {
        is_special_project_completed = sp:sp_nuclear_warheads
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = nuclear_missile_equipment
        value = 1
    }
}

nanmei_naval_invasions = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = naval_mission_threshold
        id = MISSION_CONVOY_RAIDING
        value = 50
    }
    ai_strategy = {
        type = naval_invasion_focus
        value = 25
    }
}

nanmei_this_one_came_from_here = {
    allowed = {
        or = {
            original_tag = PAM
            original_tag = PAO
            original_tag = LHS
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        OR = {
            threat > 0.80
            has_war = yes
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = 50
    }
}

PAM_unit= { #单位倾向
    allowed = {
        original_tag = PAM
    }
    enable = {
        num_divisions < 800
    }

    abort = {
        always = no
    }

    ai_strategy = {
        type = air_factory_balance
        value = 10
    }
    ai_strategy = { #驻军
        type = role_ratio
        id = garrison
        value = 0
    }
    ai_strategy = { #空降
        type = role_ratio
        id = paratroopers
        value = 0
    }

    ai_strategy = {
        type = role_ratio
        id = mountaineers_PAM
        value = 0
    }

    ai_strategy = { #非正规部队
        type = role_ratio
        id = PAM_infantry
        value = 500
    }
    ai_strategy = {
        type = build_army
        id = PAM_infantry
        value = 400
    }

    ai_strategy = {
        type = role_ratio
        id = cavalry
        value = 0
    }
    ai_strategy = { #非正规部队
        type = template_prio
        id = PAM_infantry
        value = 500

    }
    ai_strategy = {
        type = area_priority
        id = north_america
        value = 100
    }
    ai_strategy = {
        type = railway_gun_divisions_ratio
        value = 5
    }
}

PAM_unit_buffer_for_asian_threat = {#退守战场
    allowed = {
        original_tag = PAM
    }
    enable = {
        OR = { #Always have a small unit buffer along the railway, just in case asian fronts get dangerous
            has_war = no
            AND = {
                has_war = yes
                surrender_progress < 0.75
            }
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = put_unit_buffers

        # ratio of total armeis in country to be buffered
        ratio = 0.2

        # you can specify an order id. ratio of same orders ids will be share same ratio
        #order_id = 2

        # states to put garrison orders (if no state is friendly, strat is invalid)
        states = {
            487
            688
            301
            847
            900

        }

        # ai areas that the orders will use these buffers in
        area = europe
         area = asia
         area = africa
         area = south_america
         area = north_america

        # by default if you have orders in target areas, the buffer will request less units.
        # you can override this to disable the feature
        subtract_invasions_from_need = no
        subtract_fronts_from_need = yes
    }
}
PAM_civ = { #前期别产飞机
    allowed = {
        original_tag = PAM
    }
    enable = {
        date < 1938.1.1
    }

    abort = { date > 1938.1.1 }

    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = -999
    }
    # ai_strategy = {
        # type = equipment_production_factor #飞机不急着产
        # id = strike_witches_equipment
        # value = -999
    # }
    # ai_strategy = {
        # type = equipment_production_factor #飞机不急着产
        # id = strike_witches_equipment
        # value = -999
    # }
    ai_strategy = {
        type = equipment_production_factor #别造坦克
        id = armor
        value = -999
    }
}
PAM_no_wants_dockyard  = { #别造船厂
    allowed = {
        original_tag = PAM
    }
    enable = {
        not = {
            has_country_flag = PAM_nanmeitongyi
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = building_target
        id = dockyard
        value = -999
    }
}
PAM_air_production = { #空军生产

    allowed = {
        original_tag = PAM
    }

    enable = {
        always = yes
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type  = unit_ratio
        id    = fighter
        value = 40
    }
    ai_strategy = {
        type  = unit_ratio
        id    = cas
        value = 20
    }
    ai_strategy = {
        type  = unit_ratio
        id    = tactical_bomber
        value = 10
    }
    ai_strategy = {
        type  = unit_ratio
        id    = strategic_bomber
        value = 0
    }
    ai_strategy = {
        type  = unit_ratio
        id    = scout_plane
        value = 5
    }
    ai_strategy = {
        type  = unit_ratio
        id    = maritime_patrol_plane
        value = 10
    }


}


always_prioritize_our_core = {
    allowed={
        or = {
            original_tag = PAM
            original_tag = PAU
        }
    }
    enable = {
        surrender_progress > 0.05
    }

    abort = {
        OR = {
            surrender_progress < 0.01
            has_capitulated = yes
        }
    }

    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            NOT = { is_core_of = FROM.FROM }
        }
        value = -3
    }


}


PAM_avoid_thin_lines = {
    allowed = { original_tag = PAM }
    enable = {
        has_war = yes
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_controlled_by = PAM
            check_variable = {
                num_enemy_neighbor_states > 2
            }
        }
        value = 100
    }


    ai_strategy = {
        type = front_control
        state_trigger = {
            NOT = { is_core_of = FROM } #
            check_variable = { state_strategic_value < 10 }
        }
        execution_type = careful
        priority = -50
    }
}
PAM_state_is_zhanlue = {#老爷爷，我给你踩地来咯
    allowed={
        or = {
            original_tag = PAM
        }
    }
    enable = {
        or = {
            any_enemy_country = {
                strength_ratio = {
                    tag = PAM
                    ratio < 1.4
                }
            }
        }
    }
    abort = {
        and = {
            or = {
                any_enemy_country = {
                strength_ratio = {
                    tag = PAM
                    ratio < 1.4
                }
            }
            }
        }

    }
    ai_strategy = {
        type = front_control
        state_trigger = {
            is_coastal = yes
            controller = { has_war_with = PAM }
        }
        ratio = 0.2
        priority = 20000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size = 0 }
            }
        }
        priority = 9000
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size < 3 }
            }
        }

        ratio = 0.8

        priority = 5000
        ordertype = front
        execution_type = rush
        execute_order = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }
        }


        priority = 8000
        ordertype = front
        execution_type = rush
    }
}

PAU_state_is_zhanlue = {#老爷爷，我给你踩地来咯(军团)
    allowed={
        or = {
            original_tag = PAU
        }
    }
    enable = {
        or = {
            strength_ratio = {
                tag = PAU
                ratio > 0.7
            }
        }
        LHS = {
            is_ai = no
        }
    }
    abort = {
        and = {
            or = {
                strength_ratio = {
                    tag = PAU
                    ratio < 0.7
                }
            }
        }

    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            OR = {
                is_coastal = yes
            }
        }
        ratio = 0.2
        priority =20000
        ordertype = front
        execution_type = rush
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size = 0 }
            }
        }
        priority = 9000
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size < 3 }
            }
        }

        ratio = 0.8

        priority = 5000
        ordertype = front
        execution_type = rush
        execute_order = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }
        }


        priority = 8000
        ordertype = front
        execution_type = rush
    }
}
PAM_end_pocket = {#包围快速歼灭
    allowed={
        original_tag = PAM
    }
    enable = {
        always = yes
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        state_trigger = {
            OR = {
                AND = {
                    is_controlled_by = FROM.FROM
                    NOT = {
                        is_fully_controlled_by = FROM.FROM
                    }
                }
                controller = {
                    has_war_with = FROM.FROM
                }
            }
            all_neighbor_state = {
                controller = {
                    NOT = {
                        has_war_with = FROM.FROM
                    }
                }
            }
        }
        ratio = 0.0
        priority = 9999
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes

    }

    ai_strategy = {

        type = front_unit_request

        state_trigger = {
            OR = {
                AND = {
                    is_controlled_by = FROM.FROM
                    NOT = {
                        is_fully_controlled_by = FROM.FROM
                    }
                }
                controller = {
                    has_war_with = FROM.FROM
                }
            }
            all_neighbor_state = {
                controller = {
                    NOT = {
                        has_war_with = FROM.FROM
                    }
                }
            }
        }
        value = 40
    }
}



PAM_concentrate_troops = {#集中突破
    allowed = {
        original_tag = PAM
    }
    enable = {
        always = yes
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = force_concentration_factor
        value = 15  # base + 15 = 30 %
    }

}


PAM_build_an_air_facility_faster = {#建造空军研究设施
    allowed = {
        original_tag = PAM
    }
    enable = {

        is_major = yes # Minors don't bother until strong enough to be considered major
        num_of_military_factories > 40 # We should first build up our military concentrated_industry

        OR = { # Don't start until we can build decent aircraft
            has_tech = basic_small_airframe
            has_tech = basic_medium_airframe
            has_tech = basic_large_airframe
        }
        air_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = air_facility
        value = 1000
    }
}

PAM_build_a_lw_facility_faster = {#建造陆军研究设施
    allowed = {
        original_tag = PAM
    }
    enable = {
        is_major = yes
        num_of_civilian_factories > 40
        num_of_military_factories > 40
        #has_completed_focus = PAM_nzsj
        OR = { # Don't start until we can build decent tanks of some kind
            has_tech = improved_light_tank_chassis
            has_tech = basic_medium_tank_chassis
            has_tech = improved_heavy_tank_chassis
        }
        land_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = land_facility
        value = 1000
    }
}





PAM_build_a_nav_facility_faster = {#建造海军研究设施
    allowed = {
        original_tag = PAM
    }
    enable = {
        num_of_naval_factories > 25 # Only Naval Powers should bother with these unless specifically told to do otherwise
        naval_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = naval_facility
        value = 1000
    }
}


PAM_build_a_nl_facility_faster = {#建造核研究设施
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_tech = atomic_research
        num_of_civilian_factories > 25
        num_of_military_factories > 25 # Even if we have the base tech, let's wait until we have some kind of capacity
        nuclear_facility < 1  # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = nuclear_facility
        value = 1000
    }
}

PAM_recruit_land_scientist_faster = { #招募陆战科学家
    allowed = {
        original_tag = PAM
    }
    enable = {
        land_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_land
        value = 1000
    }
}

PAM_recruit_nuclear_scientist_faster = {#招募核研究科学家
    allowed = {
        original_tag = PAM
    }
    enable = {
        nuclear_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_nuclear
        value = 1000
    }
}

PAM_recruit_naval_scientist_faster = {#招募海战科学家
    allowed = {
        original_tag = PAM
    }
    enable = {
        naval_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_naval
        value = 1000
    }
}

PAM_recruit_air_scientist_faster = {#招募空战科学家
    allowed = {
        original_tag = PAM
    }
    enable = {
        air_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_air
        value = 1000
    }
}


PAM_war_with_PAU = { #与军团开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        country_exists = PAU
        NOT = {has_war_with = PAM}
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = garrison
        value = 100                     # 驻军需求（默认70-100）
    }

    ai_strategy = {
        type = front_control
        tag = PAU
        priority = 10000
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes

    }


}




PAM_check_omega_lul = { #部队逻辑
    allowed = {
        original_tag = PAM
    }
    enable = {
        always = yes
    }
    abort = {
        always = no
    }
    ai_strategy = {#不待没补给的地方
        type = front_unit_request
        state_trigger = {
            is_controlled_by = FROM.FROM
            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }
        }
        value = 15
    }
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_controlled_by = FROM
            NOT = {
                any_province_building_level = {
                    province = {
                        all_provinces = yes
                    }
                    building = supply_node
                    level > 0
                }
            }
        }
        value = -15
    }
    ai_strategy = {#保护首都
        type = front_unit_request
        state_trigger = {
            OR = {
                is_capital = yes
                any_neighbor_state = { is_capital = yes }
            }
        }
        value = 15
    }
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_capital = yes
            owner = { tag = FROM.FROM }
        }
        value = 50
    }


    ai_strategy = { #登陆更有价值的地块
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            check_variable = { state_strategic_value < 3 }
        }
        value = -5
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
            check_variable = { state_strategic_value < 8 }
        }
        value = -3
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
        }
        value = -3
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
            NOT = {
                any_province_building_level = {
                    province = {
                        all_provinces = yes
                    }
                    building = supply_node
                    level > 0
                }
            }
        }
        value = -15
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }

            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }

        }
        value = 15
    }

}




PAM_war_with_LHS = { #与野火
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_war_with = LHS
        NOT ={ has_war_with = CHL }
        LHS = {
            is_ai = no
        }

    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = invade		#入侵
        id = "LHS"
        value = 200
    }

    ai_strategy = {
        type = front_control
        tag = LHS

        priority = 75
        ordertype = front
        manual_attack = no
        execution_type = rush_weak
        execute_order = yes
        #occupation_policy = 3
    }
    ai_strategy = {
        type = garrison
        value = -100                # 驻军需求（默认70-100）
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = "LHS"
        value = 5 # lets put more units, should be safe
    }
    ai_strategy = {
        type = front_armor_score
        id = "LHS"
        value = 10
    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_controlled_by = LHS
            OR = {
                is_coastal = yes
                is_core_of = LHS
            }
        }
        ratio = 0.2
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "MRB"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MGC"
        value = -1000
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "MGU"
        value = -1000
    }


    ai_strategy = {
        type = force_defend_ally_borders
        id = "ELF"
        value = -1000
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "MRB"
        value = -1000
    }



}
PAM_war_with_LHS = { #与野火
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_war_with = LHS
        NOT ={ has_war_with = CHL }
        LHS = {
            is_ai = yes
        }

    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = invade		#入侵
        id = "LHS"
        value = 200
    }

    ai_strategy = {
        type = front_control
        tag = LHS

        priority = 75
        ordertype = front
        manual_attack = no
        execution_type = rush_weak
        execute_order = yes
        #occupation_policy = 3
    }
    ai_strategy = {
        type = garrison
        value = -100                # 驻军需求（默认70-100）
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = LHS
        value = 9999 # lets put more units, should be safe
    }
    ai_strategy = {
        type = front_armor_score
        id = LHS
        value = 100
    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_controlled_by = LHS
            OR = {
                is_coastal = yes
                is_core_of = LHS
            }
        }
        ratio = 0.2
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "MRB"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MGC"
        value = -1000
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "MGU"
        value = -1000
    }


    ai_strategy = {
        type = force_defend_ally_borders
        id = "ELF"
        value = -1000
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "MRB"
        value = -1000
    }



}










PAM_war_with_MS1 = { #与魔女之夜开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_war_with = MS1


    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        tag = MS1

        priority = 75
        ordertype = front
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes
    }
    ai_strategy = {
        type = garrison
        value = -100                    # 驻军需求（默认70-100）
    }
    ai_strategy = {
        type = front_armor_score
        id = MS1
        value = 100
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = MS1
        value = 9999 # lets put more units, should be safe
    }


}
PAM_war_with_WL1 = { #与躁动的亡灵开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_war_with = WL1
        NOT ={has_war_with = MGU }

    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        tag = WL1

        priority = 75
        ordertype = front
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes
    }
    ai_strategy = {
        type = garrison
        value = -100                    # 驻军需求（默认70-100）
    }
    ai_strategy = {
        type = front_armor_score
        id = WL1
        value = 100
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = WL1
        value = 9999 # lets put more units, should be safe
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PAQ"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MRB"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MGC"
        value = -1000
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "PGM"
        value = -1000
    }

}

PAM_war_with_BRA = { #与妖精开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        has_war_with = ELF
        NOT ={has_war_with = MGU
        has_war_with = PAM}

    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        tag = ELF

        priority = 75
        ordertype = front
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes
    }
    ai_strategy = {
        type = garrison
        value = -100                    # 驻军需求（默认70-100）
    }
    ai_strategy = {
        type = front_armor_score
        id = ELF
        value = 100
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PAQ"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PAY"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MRB"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MGC"
        value = -1000
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = ELF
        value = 9999 # lets put more units, should be safe
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PGM"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PA1"
        value = -1000
    }

}



PAM_war_with_MGU = { #与魔法少女联盟开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        AND = {
            has_war_with = MGU
            not ={ has_war_with = PAM}
            not ={ has_war_with = ELF}
        }

    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = invade		#入侵
        id = "MGU"
        value = 200
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = MGU
        value = 9999 # lets put more units, should be safe
    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_controlled_by = MGU
            OR = {
                is_coastal = yes
                is_core_of = MGU
            }
        }
        ratio = 0.2
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
    }
    ai_strategy = {
        type = front_control
        tag = MGU

        priority = 75
        ordertype = front
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes
    }
    ai_strategy = {
        type = front_armor_score
        id = MGU
        value = 100
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MRB"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "MGC"
        value = -1000
    }

    ai_strategy = {
        type = garrison
        value = -100                    # 驻军需求（默认70-100）
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PAQ"
        value = -1000
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "PGM"
        value = -1000
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "ELF"
        value = -1000
    }
}


PAM_war_with_BRA_MGU = { #与妖精和魔法少女联盟开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        AND = {
            has_war_with = ELF
            has_war_with = MGU
        }
        not = {
            has_war_with = PAM
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = invade		#入侵
        id = "ELF"
        value = 200
    }
    ai_strategy = {
        type = front_control
        tag = ELF

        priority = 75
        ordertype = front
        manual_attack = no
        execution_type = rush
        execute_order = yes
        #occupation_policy = 3
    }
    ai_strategy = {
        type = front_armor_score
        id = ELF
        value = 100
    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = ELF
        value = 80 # lets put more units, should be safe
    }
    ai_strategy = {
        type = garrison
        value = -100                    # 驻军需求（默认70-100）
    }
    ai_strategy = {
        type = force_defend_ally_borders
        id = "PAQ"
        value = -1000
    }

    ai_strategy = {
        type = force_defend_ally_borders
        id = "PGM"
        value = -1000
    }

}
PAM_war_with_PAQ = { #与阿德莱希开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        AND = {
            has_war_with = PAQ
        }

    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = invade		#入侵
        id = "PAQ"
        value = 200
    }

    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_controlled_by = PAQ
            OR = {
                is_coastal = yes
            }
        }
        ratio = 0.2
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
        #occupation_policy = 3
    }
    ai_strategy = {
        type = front_control
        tag = PAQ

        priority = 75
        ordertype = front
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes
    }
    ai_strategy = {
        type = front_armor_score
        id = PAQ
        value = 100
    }
    ai_strategy = {
        type = garrison
        value = -100                   # 驻军需求（默认70-100）
    }
}
PAM_war_with_PAQ = { #与阿德莱希开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        AND = {
            has_war_with = PAQ
        }

    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = invade		#入侵
        id = "PAQ"
        value = 200
    }

    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_controlled_by = PAQ
            OR = {
                is_coastal = yes
            }
        }
        ratio = 0.2
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
        #occupation_policy = 3
    }
    ai_strategy = {
        type = front_control
        tag = PAQ

        priority = 75
        ordertype = front
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes
    }
    ai_strategy = {
        type = front_armor_score
        id = PAQ
        value = 100
    }
    ai_strategy = {
        type = garrison
        value = -100                   # 驻军需求（默认70-100）
    }
}




PAM_war_with_PAY = { #与魔女之树开战
    allowed = {
        original_tag = PAM
    }
    enable = {
        AND = {
            has_war_with = PAY
        }

    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = invade		#入侵
        id = "PAY"
        value = 200
    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_controlled_by = PAY
            OR = {
                is_coastal = yes
            }
        }

        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
        #occupation_policy = 3
    }
    ai_strategy = {
        type = invasion_unit_request
        state = 304
        value = 9999

    }
    ai_strategy = {
        type = front_control
        tag = PAY

        priority = 75
        ordertype = front
        execution_type = careful
        #occupation_policy = 3
        execute_order = yes
    }
    ai_strategy = {
        type = front_armor_score
        id = PAY
        value = 100
    }
    ai_strategy = {
        type = garrison
        value = -100                    # 驻军需求（默认70-100）
    }
}




PAM_zhunbei_LHS = { #提前准备攻打野火
    enable = {
        tag = PAM
        date > 1938.1.1
    }
    abort = {
        not = { country_exists = PAM }
    }

    ai_strategy = {
        type = prepare_for_war
        id = "LHS"
        value = 100
    }
}
PAM_zhunbei_MS1 = { #提前准备攻打魔女之夜
    enable = {
        tag = PAM
        always = no
        # OR = {
            # focus_progress = {
                # focus = PAM_wckq
                # progress > 0
            # }
            # focus_progress = {
                # focus = PAM_zonggong
                # progress > 0
            # }
# 
        # }

    }
    abort = {
        not = { country_exists = MS1 }
    }

    ai_strategy = {
        type = prepare_for_war
        id = "MS1"
        value = 100
    }
}




PAM_zhunbei_WL1 = { #提前准备攻打躁动亡灵
    enable = {
        tag = PAM
        OR = {
            focus_progress = {
                focus = PAM_cztm
                progress > 0
            }

        }
    }
    abort = {
        not = { country_exists = WL1 }

    }
    ai_strategy = {
        type = front_unit_request #前线部队
        tag = WL1
        value = 9999 # lets put more units, should be safe
    }
    ai_strategy = {
        type = prepare_for_war
        id = "WL1"
        value = 100
    }


}

PAM_zhunbei_ELF = { #提前准备攻打妖精
    enable = {
        tag = PAM
        always = no
        # OR = {
            # focus_progress = {
                # focus = PAM_zhuiji
                # progress > 0
            # }
            # focus_progress = {
                # focus = PAM_qpdy
                # progress > 0
            # }
# 
        # }

    }
    abort = {
        not = { country_exists = ELF }
    }

    ai_strategy = {
        type = prepare_for_war
        id = "ELF"
        value = 1000
    }
    ai_strategy = {
        type = prepare_for_war
        id = "ELG"
        value = 1000
    }
    ai_strategy = {
        type = prepare_for_war
        id = "EDW"
        value = 1000
    }
    ai_strategy = {
        type = prepare_for_war
        id = "ELW"
        value = 1000
    }
    ai_strategy = {
        type = prepare_for_war
        id = "EWW"
        value = 1000
    }
    ai_strategy = {
        type = prepare_for_war
        id = "EBS"
        value = 1000
    }
    ai_strategy = {
        type = prepare_for_war
        id = "EFC"
        value = 1000
    }

}
PAM_diplomacy_with_PAY  = { #干魔女

    allowed = {
        original_tag = PAM
    }

    enable = {
        PAY = {
            not = {
                has_government = communism
            }
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = declare_war
        id = PAY
        value = 100
    }
    ai_strategy = {
        type = antagonize
        id = PAY
        value = 100
    }

    ai_strategy = {
        type = conquer
        id = PAY
        value = 50
    }
}








#对莉莉安娜示好
PAM_PAO_is_friend = {
    allowed = {
        original_tag = PAM
    }
    enable = {
        date > 1939.1.1
        PAO = {
            exists = yes
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = befriend
        id = "PAO"
        value = 200
    }
    ai_strategy = {
        type = alliance
        id = "PAO"
        value = 200
    }


    ai_strategy = { #帮填线
        type = force_defend_ally_borders
        id = "PAO"
        value = 20
    }
    ai_strategy = {
        type = diplo_action_desire
        id = PAO
        target = military_access
        value = 1000
    }
}
PAM_diplomacy_with_LHS = { #对抗洛社

    allowed = {
        original_tag = PAM
    }

    enable = {
        NOT = {
            has_government = communism
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = antagonize
        id = LHS
        value = 100
    }

    ai_strategy = {
        type = conquer
        id = LHS
        value = 50
    }
    ai_strategy = {
        type = declare_war
        id = LHS
        value = 100
    }
}





#优先升编制
PAM_xp_upgrade_spending_land = {
    allowed = {
        original_tag = PAM
    }

    enable = {
        always = yes
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = land_xp_spend_priority
        id = upgrade_xp_cutoff
        value = 100
    }
    ai_strategy = {
        type = land_xp_spend_priority
        id = division_template
        value = 200
    }
}