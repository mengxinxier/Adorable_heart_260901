ELF_start1 = {
    enable = {
        OR = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = force_defend_ally_borders
        id = "WRQ"
        value = -1000
    }
    ai_strategy = {
        type = become_spymaster
        value = 30  # 花费政治点数竞选间谍大师的权重值
    }
    ai_strategy = {
        type = garrison
        value = -200
    }
    ai_strategy = { #注重船坞
        type = front_control
        state_trigger = {
            is_coastal = yes
            FROM = { has_war_with = FROM.FROM }
        }
        priority = 120
        ordertype = front
        execution_type = rush
        manual_attack = no
        execute_order  = yes
    }
    ai_strategy = {
        type = building_target
        id = supply_node
        value = 200
    }
    ai_strategy = {
        type = research_weight_factor	#重坦
        id = basic_heavy_tank_chassis
        value = -999
    }
    ai_strategy = {
        type = research_tech	#工业
        id = concentrated_industry
        value = 200
    }
    ai_strategy = {
        type = research_weight_factor	#科技倾向
        id = basic_battery
        value = -200
    }
    ai_strategy = {
        type = research_weight_factor	#科技倾向
        id = aircraft_construction
        value = -200
    }
    ai_strategy = {
        type = research_weight_factor	#科技倾向
        id = engines_1
        value = -200
    }
    ai_strategy = {
        type = research_weight_factor	#科技倾向
        id = range_improvements
        value = -200
    }
    ai_strategy = {
        type = research_tech	#电子学
        id = electronic_mechanical_engineering
        value = 200
    }
}

More_Infantry_focus = {#多产枪
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        arms_factory > 40
        arms_factory < 69
        has_equipment = { infantry_equipment < 1000 }
    }

    abort_when_not_enabled = yes


    ai_strategy = {
        type = equipment_production_factor
        id = infantry
        value = 50
    }

}
ELF_stop_disbanding_your_army = {#ai部署陆军倾向
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        num_divisions < 800
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 1000
    }
}
ELF_stop_disbanding_your_army2 = {#ai部署陆军倾向2
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        num_divisions > 800
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = -2000
    }
}
#1939年研究
ELF_ai_research = {
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        date > 1939.1.1
    }
    abort = {
        always = no
    }
    ai_strategy = { #36年步兵装备
        type = research_tech
        id = infantry_weapons1
        value = 2000
    }
    ai_strategy = { #大威力步枪
        type = research_tech
        id = infantry_weapons2
        value = 2000
    }
    ai_strategy = { #40年步兵装备
        type = research_tech
        id = improved_infantry_weapons
        value = 2000
    }
    ai_strategy = { #40年步兵装备2
        type = research_tech
        id = improved_infantry_weapons_2
        value = 2000
    }
    ai_strategy = { #42年步兵装备
        type = research_tech
        id = advanced_infantry_weapons
        value = 2000
    }
    ai_strategy = { #42年步兵装备
        type = research_tech
        id = advanced_infantry_weapons2
        value = 2000
    }
    ai_strategy = { #火炮
        type = research_tech
        id = gw_artillery
        value = 200
    }
    ai_strategy = { #火炮2
        type = research_tech
        id = interwar_artillery
        value = 200
    }

    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = artillery1
        value = 200
    }
    ai_strategy = { #QF 25磅野战炮
        type = research_tech
        id = artillery2
        value = 200
    }
    ai_strategy = { #工兵连 II
        type = research_tech
        id = tech_engineers2
        value = 200
    }
    ai_strategy = { #维修连 II
        type = research_tech
        id = tech_maintenance_company2
        value = 400
    }
    ai_strategy = { #后勤连 II
        type = research_tech
        id = tech_logistics_company2
        value = 300
    }
}

#科技卡研究
ELF_ai_research2 = { #
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        #has_completed_focus = ELF_shzg
        always = no
    }
    abort = {
        always = no
    }
    ai_strategy = { #1939潜艇
        type = research_tech
        id = improved_ship_hull_submarine
        value = 300
    }
    ai_strategy = { #1940潜艇
        type = research_tech
        id = advanced_ship_hull_submarine
        value = 300
    }
}
#科技卡研究
ELF_ai_research3 = { #
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        #has_completed_focus = ELF_zscb
        always = no
    }
    abort = {
        always = no
    }
    ai_strategy = { #基础强袭
        type = research_tech
        id = strike_unit2
        value = 300
    }
    ai_strategy = { #先进强袭
        type = research_tech
        id = strike_unit3
        value = 300
    }
}
ELF_motorized_production = { #缺卡车
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }

    }
    enable = {
        has_equipment = {
            motorized_equipment < 1000
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = motorized
        value = 10
    }
    ai_strategy = {
        type = equipment_variant_production_factor
        id = motorized_equipment
        value = 50
    }
}
ELF_artillery_production = { #缺炮
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }

    }
    enable = {
        has_equipment = {
            artillery_equipment < 200
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = artillery
        value = 5
    }
}
ELF_anti_tank_production = { #缺at
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }

    }
    enable = {
        has_equipment = {
            anti_tank_equipment < 100
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = anti_tank
        value = 5
    }
}
ELF_anti_air_production = { #缺对空
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }

    }
    enable = {
        has_equipment = {
            anti_air_equipment < 200
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = anti_air
        value = 5
    }
}
ELF_train_production = { #缺火车
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }

    }
    enable = {
        has_equipment = {
            train_equipment  < 50
        }
        num_of_military_factories > 50
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = train
        value = 8
    }
}
ELF_train_production = { #缺运输船
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }

    }
    enable = {
        has_equipment = {
            convoy  < 500
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories
        id = convoy
        value = 10
    }
}

ELF_wants_arms_factory = { #搓军工
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        num_of_military_factories < 300
    }
    abort = {
        num_of_military_factories > 300
    }
    ai_strategy = {
        type = building_target
        id = arms_factory
        value = 100
    }
}


ELF_wants_arms_factory2 = { #狠狠搓军工
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        date > 1938.9.1
        OR = {
            and = {
                num_of_military_factories < 300
            }
        }
    }
    abort = {
        num_of_military_factories > 300
    }
    ai_strategy = {
        type = building_target
        id = arms_factory
        value = 10000
    }
}





ELF_wants_civ = {#搓民工
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        date < 1938.9.1
        has_war = no
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = building_target
        id = industrial_complex
        value = 10000
    }
}



ELF_surplus_management = {#存装备

    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        always = yes
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_surplus_management
        id = infantry_equipment
        value = 10
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = support_equipment
        value = 5
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = artillery_equipment
        value = 5
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = anti_air_equipment
        value = 3
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = anti_tank_equipment
        value = 2
    }

    ai_strategy = {
        type = equipment_production_surplus_management
        id = convoy
        value = 5
    }
}
ELF_market_convoy_purchases = {#买运输船
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        naval_base > 0
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_market_buying_threshold
        id = convoy
        value = 30
    }
}
ELF_market_stockpile_management_if_at_war = {#战时市场
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_war = yes

    }
    abort_when_not_enabled = yes


    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = infantry
        value = 20000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = support
        value = 6000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = artillery
        value = 1000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = fighter
        value = 200
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = motorized
        value = 1000
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = train
        value = 50
    }
    ai_strategy = {
        type = equipment_market_for_sale_threshold
        id = convoy
        value = 50
    }
}
ELF_stockpile_management = {#装备库存保持
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        num_divisions > 150
    }

    abort = {
        num_divisions < 130
    }

    ai_strategy = {
        type = equipment_stockpile_surplus_ratio
        value = 30
    }
}

ELF_if_we_can_build_v1_rockets_we_want_rockets = {#生产v1导弹
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        is_special_project_completed = sp:sp_rockets_flying_bomb
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = guided_missile_equipment
        value = 1
    }
}

ELF_if_we_can_build_v2_rockets_we_want_v2_rockets = {#生产v2导弹
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        is_special_project_completed = sp:sp_rockets_ballistic_missile
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = ballistic_missile_equipment
        value = 1
    }
}
ELF_if_we_can_build_sams_we_want_sams = { #生产防空导弹
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        is_special_project_completed = sp:sp_rockets_ground_to_air_missile
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = sam_missile_equipment
        value = 1
    }
}
ELF_if_we_can_build_nuclear_missiles_we_want_nuclear_missiles = {#生产核弹
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        is_special_project_completed = sp:sp_nuclear_warheads
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = equipment_production_min_factories_archetype
        id = nuclear_missile_equipment
        value = 1
    }
}


ELF_unit= { #单位倾向
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        num_divisions < 800
    }

    abort = {
        always = no
    }

    ai_strategy = {
        type = air_factory_balance
        value = 10
    }
    ai_strategy = { #驻军
        type = role_ratio
        id = garrison
        value = 0
    }
    ai_strategy = { #空降
        type = role_ratio
        id = paratroopers
        value = 0
    }

    ai_strategy = {
        type = role_ratio
        id = mountaineers_ELF
        value = 0
    }

    ai_strategy = { #非正规部队
        type = role_ratio
        id = ELF_infantry
        value = 500
    }
    ai_strategy = {#非正规部队
        type = build_army
        id = ELF_infantry
        value = 400
    }

    ai_strategy = {
        type = role_ratio
        id = cavalry
        value = 0
    }

    ai_strategy = {
        type = template_prio
        id = infantry_ELF2
        value = 0

    }
    ai_strategy = {
        type = role_ratio
        id = infantry_ELF2
        value = 0

    }
    ai_strategy = {
        type = template_prio
        id = armor
        value = 0
    }
    ai_strategy = {
        type = role_ratio
        id = armor
        value = 0
    }
    ai_strategy = { #非正规部队
        type = template_prio
        id = ELF_infantry
        value = 500

    }
    ai_strategy = {
        type = area_priority
        id = north_america
        value = 100
    }
    ai_strategy = {
        type = railway_gun_divisions_ratio
        value = 5
    }
}

ELF_unit_buffer_for_asian_threat = {#退守战场
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        OR = { #Always have a small unit buffer along the railway, just in case asian fronts get dangerous
            has_war = no
            AND = {
                has_war = yes
                surrender_progress < 0.5
            }
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = put_unit_buffers

        # ratio of total armeis in country to be buffered
        ratio = 0.1

        # you can specify an order id. ratio of same orders ids will be share same ratio
        #order_id = 2

        # states to put garrison orders (if no state is friendly, strat is invalid)
        states = {
            487
            688
            301
            847
            900
        }
        area = europe
        area = asia
        area = africa
        area = south_america
        area = north_america
        # ai areas that the orders will use these buffers in
        # by default if you have orders in target areas, the buffer will request less units.
        # you can override this to disable the feature
        subtract_invasions_from_need = no
        subtract_fronts_from_need = yes
    }
}
ELF_civ = { #前期别产飞机
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        date < 1938.1.1
    }

    abort = { date > 1938.1.1 }

    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = -999
    }
    ai_strategy = {
        type = equipment_production_factor #飞机不急着产
        id = fighter
        value = -999
    }
    ai_strategy = {
        type = equipment_production_factor #飞机不急着产
        id = cas
        value = -999
    }
    ai_strategy = {
        type = equipment_production_factor #别造坦克
        id = armor
        value = -999
    }
}
ELF_no_wants_dockyard  = { #别造船厂
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        not = {
            has_country_flag = ELF_nanmeitongyi
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = building_target
        id = dockyard
        value = -999
    }
}
ELF_air_production = { #空军生产

    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }

    enable = {
        always = yes
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type  = unit_ratio
        id    = fighter
        value = 40
    }
    ai_strategy = {
        type  = unit_ratio
        id    = cas
        value = 20
    }
    ai_strategy = {
        type  = unit_ratio
        id    = tactical_bomber
        value = 10
    }
    ai_strategy = {
        type  = unit_ratio
        id    = strategic_bomber
        value = 0
    }
    ai_strategy = {
        type  = unit_ratio
        id    = scout_plane
        value = 5
    }
    ai_strategy = {
        type  = unit_ratio
        id    = maritime_patrol_plane
        value = 10
    }


}

always_prioritize_our_core = {
    allowed={
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        surrender_progress > 0.05
    }
    abort = {
        OR = {
            surrender_progress < 0.01
            has_capitulated = yes
        }
    }

    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            NOT = { is_core_of = FROM.FROM }
        }
        value = -3
    }


}

ELF_state_is_zhanlue = {#老爷爷，我给你踩地来咯
    allowed={
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        or = {
            has_equipment = { infantry_equipment > -2000 }
            has_equipment = { magic_assault_equipment > -2000 }
        }
    }
    abort = {
        and = {
            or = {
                has_equipment = { infantry_equipment < -2000 }
                has_equipment = { magic_assault_equipment < -2000 }
            }
        }

    }
    ai_strategy = {
        type = front_control
        state_trigger = {
            is_coastal = yes
            controller = { has_war_with = ELF }
        }
        ratio = 0.2
        priority = 20000
        ordertype = front
        execution_type = rush
        execute_order = yes
        manual_attack = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size = 0 }
            }
        }
        priority = 9000
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            FROM = {
                divisions_in_state = { state = ROOT size < 3 }
            }
        }

        ratio = 0.8

        priority = 5000
        ordertype = front
        execution_type = rush
        execute_order = yes
    }
    ai_strategy = {
        type = front_control

        state_trigger = {
            FROM = { has_war_with = FROM.FROM }
            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }
        }


        priority = 8000
        ordertype = front
        execution_type = rush
    }
}
ELF_end_pocket = {#包围快速歼灭
    allowed={
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        always = yes
    }

    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_control
        state_trigger = {
            OR = {
                AND = {
                    is_controlled_by = FROM.FROM
                    NOT = {
                        is_fully_controlled_by = FROM.FROM
                    }
                }
                controller = {
                    has_war_with = FROM.FROM
                }
            }
            all_neighbor_state = {
                controller = {
                    NOT = {
                        has_war_with = FROM.FROM
                    }
                }
            }
        }
        ratio = 0.0
        priority = 9999
        ordertype = front
        execution_type = rush_weak
        execute_order = yes
        manual_attack = yes

    }

    ai_strategy = {

        type = front_unit_request

        state_trigger = {
            OR = {
                AND = {
                    is_controlled_by = FROM.FROM
                    NOT = {
                        is_fully_controlled_by = FROM.FROM
                    }
                }
                controller = {
                    has_war_with = FROM.FROM
                }
            }
            all_neighbor_state = {
                controller = {
                    NOT = {
                        has_war_with = FROM.FROM
                    }
                }
            }
        }
        value = 40
    }
}

ELF_concentrate_troops = {#集中突破
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        always = yes
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = force_concentration_factor
        value = 15  # base + 15 = 30 %
    }

}


ELF_build_an_air_facility_faster = {#建造空军研究设施
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {

        is_major = yes # Minors don't bother until strong enough to be considered major
        num_of_military_factories > 200 # We should first build up our military industry

        OR = { # Don't start until we can build decent aircraft
            has_tech = basic_small_airframe
            has_tech = basic_medium_airframe
            has_tech = basic_large_airframe
        }
        air_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = air_facility
        value = 1000
    }
}

ELF_build_a_lw_facility_faster = {#建造陆军研究设施
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        is_major = yes
        num_of_civilian_factories > 40
        num_of_military_factories > 40
        #has_completed_focus = ELF_nzsj
        OR = { # Don't start until we can build decent tanks of some kind
            has_tech = improved_light_tank_chassis
            has_tech = basic_medium_tank_chassis
            has_tech = improved_heavy_tank_chassis
        }
        land_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = land_facility
        value = 1000
    }
}





ELF_build_a_nav_facility_faster = {#建造海军研究设施
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        num_of_naval_factories > 25 # Only Naval Powers should bother with these unless specifically told to do otherwise
        naval_facility < 1 # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = naval_facility
        value = 1000
    }
}


ELF_build_a_nl_facility_faster = {#建造核研究设施
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        has_tech = atomic_research
        num_of_civilian_factories > 25
        num_of_military_factories > 25 # Even if we have the base tech, let's wait until we have some kind of capacity
        nuclear_facility < 1  # one is enough
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = build_building
        id = nuclear_facility
        value = 1000
    }
}

ELF_recruit_land_scientist_faster = { #招募陆战科学家
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        land_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_land
        value = 1000
    }
}

ELF_recruit_nuclear_scientist_faster = {#招募核研究科学家
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        nuclear_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_nuclear
        value = 1000
    }
}

ELF_recruit_naval_scientist_faster = {#招募海战科学家
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        naval_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_naval
        value = 1000
    }
}

ELF_recruit_air_scientist_faster = {#招募空战科学家
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        air_facility > 0
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = recruit_scientist
        id = specialization_air
        value = 1000
    }
}
ELF_check_omega_lul = { #部队逻辑
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }
    enable = {
        always = yes
    }
    abort = {
        always = no
    }
    ai_strategy = {#不待没补给的地方
        type = front_unit_request
        state_trigger = {
            is_controlled_by = FROM.FROM
            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }
        }
        value = 15
    }
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_controlled_by = FROM
            NOT = {
                any_province_building_level = {
                    province = {
                        all_provinces = yes
                    }
                    building = supply_node
                    level > 0
                }
            }
        }
        value = -15
    }
    ai_strategy = {#保护首都
        type = front_unit_request
        state_trigger = {
            OR = {
                is_capital = yes
                any_neighbor_state = { is_capital = yes }
            }
        }
        value = 15
    }
    ai_strategy = {
        type = front_unit_request
        state_trigger = {
            is_capital = yes
            owner = { tag = FROM.FROM }
        }
        value = 50
    }


    ai_strategy = { #登陆更有价值的地块
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            check_variable = { state_strategic_value < 3 }
        }
        value = -5
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
            check_variable = { state_strategic_value < 8 }
        }
        value = -3
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
        }
        value = -3
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }
            NOT = { is_core_of = FROM.FROM }
            NOT = {
                any_province_building_level = {
                    province = {
                        all_provinces = yes
                    }
                    building = supply_node
                    level > 0
                }
            }
        }
        value = -15
    }
    ai_strategy = {
        type = invasion_unit_request
        state_trigger = {
            NOT = { has_state_flag = recent_naval_invasion_flag }

            any_province_building_level = {
                province = {
                    all_provinces = yes
                }
                building = supply_node
                level > 0
            }

        }
        value = 15
    }

}



ELF_zhunbei_PAQ = { #提前准备进攻帝国
    enable = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
        EWW = {
            OR = {
                focus_progress = {
                    focus = EWW_bzkq
                    progress > 0
                }
            }
        }

    }
    abort = {
        not = { country_exists = PAQ }
    }

    ai_strategy = {
        type = prepare_for_war
        id = "PAQ"
        value = 100
    }
}

#要军通
LHS_ask_for_access = {
    reversed = yes

    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }

    enable = {
        OR = {
            has_war_together_with = ELF
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = diplo_action_desire
        id = "ELF"
        target = military_access
        value = 1000
    }
}



ELF_diplomacy_with_LHS = { #对抗野火

    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }

    enable = {
        NOT = {
            has_government = communism
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = antagonize
        id = LHS
        value = 100
    }

    ai_strategy = {
        type = conquer
        id = LHS
        value = 50
    }
    ai_strategy = {
        type = declare_war
        id = LHS
        value = 100
    }
}

ELF_diplomacy_with_PAQ = { #对抗帝国

    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }

    enable = {
        NOT = {
            has_government = communism
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = antagonize
        id = PAQ
        value = 100
    }

    ai_strategy = {
        type = conquer
        id = PAQ
        value = 50
    }
    ai_strategy = {
        type = declare_war
        id = PAQ
        value = 100
    }
}


#防止军通杀

ELF_no_military_access = {

    reversed = yes

    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }

    enable = {
        is_ai = no
        NOT = {
            has_non_aggression_pact_with = ELF
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = diplo_action_acceptance
        id = "ELF"
        target = military_access
        value = -1000
    }

}




#优先升编制
ELF_xp_upgrade_spending_land = {
    allowed = {
        or = {
            original_tag = ELF
            original_tag = ELW
            original_tag = EWW
            original_tag = EFC
            original_tag = ELG
            original_tag = EDW
            original_tag = EBS
        }
    }

    enable = {
        always = yes
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = land_xp_spend_priority
        id = upgrade_xp_cutoff
        value = 100
    }
    ai_strategy = {
        type = land_xp_spend_priority
        id = division_template
        value = 200
    }
}